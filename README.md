# mysandbox
Flask Boilerplate is are updated base on my python flask learning progress
```bash
apt-get install python3-venv default-libmysqlclient-dev
pip install -r requirements.txt
flask db upgrade
flask run
```
